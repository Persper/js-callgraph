'use strict';

beforeEach(function () {
  jasmine.addMatchers({
    // since classList may not be supported in all browsers
    toHaveClass: function toHaveClass() {
      return {
        compare: function compare(el, cls) {
          var pass = el.classList ? el.classList.contains(cls) : el.getAttribute('class').split(/\s+/g).indexOf(cls) > -1;
          return {
            pass: pass,
            message: 'Expected element' + (pass ? ' ' : ' not ') + 'to have class ' + cls
          };
        }
      };
    }
  });
});

