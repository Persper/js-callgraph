'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.shouldDecodeNewlinesForHref = exports.shouldDecodeNewlines = undefined;

var _index = require('core/util/index');

// check whether current browser encodes a char inside attribute values
var div = void 0;

function getShouldDecode(href) {
  div = div || document.createElement('div');
  div.innerHTML = href ? '<a href="\n"/>' : '<div a="\n"/>';
  return div.innerHTML.indexOf('&#10;') > 0;
}

// #3663: IE encodes newlines inside attribute values while other browsers don't
var shouldDecodeNewlines = exports.shouldDecodeNewlines = _index.inBrowser ? getShouldDecode(false) : false;
// #6828: chrome encodes content in a[href]
var shouldDecodeNewlinesForHref = exports.shouldDecodeNewlinesForHref = _index.inBrowser ? getShouldDecode(true) : false;

