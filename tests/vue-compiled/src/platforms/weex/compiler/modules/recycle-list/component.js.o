'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.postTransformComponent = postTransformComponent;

var _helpers = require('compiler/helpers');

var _index = require('weex/util/index');

// mark components as inside recycle-list so that we know we need to invoke
// their special @render function instead of render in create-component.js
function postTransformComponent(el, options) {
  // $flow-disable-line (we know isReservedTag is there)
  if (!options.isReservedTag(el.tag) && el.tag !== 'cell-slot') {
    (0, _helpers.addAttr)(el, _index.RECYCLE_LIST_MARKER, 'true');
  }
}

