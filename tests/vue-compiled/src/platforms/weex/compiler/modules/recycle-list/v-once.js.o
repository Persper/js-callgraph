'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.preTransformVOnce = preTransformVOnce;

var _helpers = require('compiler/helpers');

function containVOnce(el) {
  for (var attr in el.attrsMap) {
    if (/^v\-once$/i.test(attr)) {
      return true;
    }
  }
  return false;
}

function preTransformVOnce(el, options) {
  if (containVOnce(el)) {
    (0, _helpers.getAndRemoveAttr)(el, 'v-once', true);
    (0, _helpers.addRawAttr)(el, '[[once]]', true);
  }
}

