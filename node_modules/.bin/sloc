#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sloc/bin/sloc" "$@"
  ret=$?
else 
  node  "$basedir/../sloc/bin/sloc" "$@"
  ret=$?
fi
exit $ret
